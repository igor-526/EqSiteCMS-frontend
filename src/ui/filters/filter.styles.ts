import { createStyles } from "antd-style";

export const useFilterStyles = createStyles(() => ({
  fieldSpacing: {
    marginBottom: 8,
    display: "block",
  },
  searchInput: {
    marginBottom: 8,
    display: "block",
  },
}));
