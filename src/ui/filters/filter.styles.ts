import { createStyles } from "antd-style";

export const useFilterStyles = createStyles(({ token }) => ({
    fieldSpacing: {
        marginBottom: 8,
        display: "block",
    },
    searchInput: {
        marginBottom: 8,
        display: "block",
    },
}));
